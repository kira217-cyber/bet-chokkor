# সার্ভার
PORT=5000
NODE_ENV=development

# ডেটাবেস — আসল কানেকশন স্ট্রিং .env এ রাখুন, কখনো কমিট করবেন না
MONGO_URI=mongodb://<user>:<password>@<host>:27017/betchokkor?authSource=admin

# JWT — `openssl rand -hex 32` দিয়ে নিজের সিক্রেট বানিয়ে নিন
JWT_SECRET=<৬৪ অক্ষরের র‍্যান্ডম স্ট্রিং>
JWT_EXPIRE=7d

# CORS — কমা দিয়ে আলাদা করা অরিজিন। খালি রাখলে সব অরিজিন allowed
# (API টোকেন-ভিত্তিক, কুকি নয় — তাই CSRF ঝুঁকি নেই)
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175

# ডেমো (view only) অ্যাকাউন্ট — লগইন পেজে দেখানো হয়
# পাসওয়ার্ড ডেটাবেসে শুধু হ্যাশ হয়ে থাকে; এখানে থাকা কপিটা কেবল
# লগইন পেজে দেখানোর জন্য। অ্যাকাউন্টটি viewer না হলে API কিছুই দেয় না।
DEMO_ADMIN_EMAIL=viewer@betchokkor.com
DEMO_ADMIN_PASSWORD=<ডেমো পাসওয়ার্ড>

# white-label master — গেম, ক্যাটাগরি ও প্রোভাইডারের উৎস
MASTER_API_URL=http://localhost:5005

# অটো ডিপোজিটের পেমেন্ট গেটওয়ে (OraclePay)
# Business token এখানে নয় — অ্যাডমিন প্যানেলের Auto Deposit পেজ থেকে
# বসে, তাই গেটওয়ে বদলালে বা টোকেন নতুন হলে ডিপ্লয় লাগে না
OPAY_URL=https://api.oraclepay.org/api/opay-business/generate-payment-page

# গেটওয়ে ও গেম প্ল্যাটফর্ম কোথায় ফেরত ডাকবে — লাইভে আসল ডোমেইন
PUBLIC_SERVER_URL=http://localhost:5000
PUBLIC_CLIENT_URL=http://localhost:5173
